"""dantectl -- terminal Dante controller (discovery + clock sync).

Read-only: browses mDNS, listens to the device-info and heartbeat multicast
groups, and asks devices for names, channel counts and clock state. It never
advertises itself as a device.

Not affiliated with, authorized by, or approved by Audinate.
"""

__version__ = "0.1.0"
